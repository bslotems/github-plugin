<j:jelly xmlns:j="jelly:core" xmlns:st="jelly:stapler" xmlns:d="jelly:define" xmlns:l="/lib/layout" xmlns:t="/lib/hudson" xmlns:f="/lib/form">
    <f:entry title="${%github.branchFilter}">
        <f:textbox name="github.branchFilter" value="${instance.branchFilter}" />
    </f:entry>
    <f:entry title="${%github.userBlacklist}">
        <f:textbox name="github.userBlacklist" value="${instance.userBlacklist}" />
    </f:entry>
</j:jelly>